//! Module for `/teleport` and `/tp` command.
//! See [`cmd_teleport`].

use bevy::ecs::message::MessageReader;
use meloncraft_command::raw::RawCommand;

pub fn cmd_teleport(mut raw_command_mr: MessageReader<RawCommand>) {

}
